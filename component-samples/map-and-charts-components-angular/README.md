# Components (Map and Charts) Angular sample

📁 **[Click here to download this directory as a ZIP file](https://esri.github.io/jsapi-resources/zips/map-and-charts-components-angular.zip)** 📁

This sample demonstrates how to use the Map and Charts components from the ArcGIS Maps SDK for JavaScript in an Angular application.

See the [Get started with npm guide](https://developers.arcgis.com/javascript/latest/get-started/#npm) for full instructions.

## Known issues

- Compile warnings for SDK dependencies on npm packages using CommonJS or AMD is a known issue and can be ignored. For the list of warnings being suppressed, see this project's `angular.json` file, under `allowedCommonJsDependencies`.
